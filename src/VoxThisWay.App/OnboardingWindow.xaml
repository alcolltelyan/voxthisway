<ui:FluentWindow x:Class="VoxThisWay.App.OnboardingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="VoxThisWay – First-Time Setup"
        Width="820"
        Height="560"
        MinWidth="720"
        MinHeight="500"
        ResizeMode="CanResize"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round"
        Foreground="{DynamicResource TextFillColorPrimaryBrush}"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="VoxThisWay – First-Time Setup" />

        <Grid Grid.Row="1" Margin="16,12,16,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock x:Name="ProgressText" Grid.Row="0" FontWeight="SemiBold" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
            <ProgressBar x:Name="ProgressBar" Grid.Row="1" Height="6" Margin="0,8,0,0" Minimum="0" Maximum="1" Value="0" />
        </Grid>

        <Frame Grid.Row="2" x:Name="ContentFrame" NavigationUIVisibility="Hidden" Margin="0" />

        <DockPanel Grid.Row="3" Margin="16" LastChildFill="False">
            <CheckBox x:Name="DontShowAgainCheckBox" Content="Don’t show this again" VerticalAlignment="Center" Foreground="{DynamicResource TextFillColorPrimaryBrush}" />

            <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BackButton" Content="Back" Width="90" Margin="0,0,8,0" Click="Back_Click" />
                <Button x:Name="NextButton" Content="Next" Width="90" Margin="0,0,8,0" Click="Next_Click" />
                <Button x:Name="OpenSettingsButton" Content="Open Settings" Width="120" Margin="0,0,8,0" Click="OpenSettings_Click" />
                <Button x:Name="RerunChecksButton" Content="Re-run checks" Width="120" Margin="0,0,8,0" Click="RerunChecks_Click" />
                <Button x:Name="CloseButton" Content="Finish" Width="90" Click="Close_Click" />
            </StackPanel>
        </DockPanel>
    </Grid>
</ui:FluentWindow>
